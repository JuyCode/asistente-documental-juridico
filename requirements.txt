# ============================================================
# Dependencias del proyecto: Asistente Documental RAG (PDFs)
# Stack: Streamlit + LlamaIndex + (Gemini | OpenAI) + Qdrant
# ============================================================

# ---- Interfaz web -------------------------------------------------
streamlit>=1.36.0

# ---- Motor RAG (LlamaIndex core) ---------------------------------
llama-index-core>=0.11.0

# ---- Proveedores LLM + Embeddings --------------------------------
# Gemini (Google AI Studio)  - recomendado: capa gratuita generosa
# Se usa el SDK unificado de Google (google-genai) vía llama-index:
# los paquetes antiguos llama-index-llms-gemini / embeddings-gemini están EOL.
llama-index-llms-google-genai>=0.11.0
llama-index-embeddings-google-genai>=0.6.0
google-genai>=2.0.0

# OpenAI (alternativa): gpt-4o-mini / text-embedding-3-small
llama-index-llms-openai>=0.3.0
llama-index-embeddings-openai>=0.3.0

# ---- Vector DB en la nube (Qdrant Cloud) --------------------------
# Si no se configuran QDRANT_URL/QDRANT_API_KEY la app usa el modo
# local persistente (carpeta ./data), apto para pruebas y demo.
llama-index-vector-stores-qdrant>=0.5.0
qdrant-client>=1.10.0

# ---- Extracción de texto de PDFs ----------------------------------
pypdf>=4.2.0

# ---- Configuración por variables de entorno -----------------------
python-dotenv>=1.0.1